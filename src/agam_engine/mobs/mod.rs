
use crate::prelude::*;

mod components;

/// A plugin to manage:
/// - mob rendering, mob behavior, mob interactions, status effects 
pub struct AgamMobPlugin;
impl Plugin for AgamMobPlugin {
    fn build(&self, app: &mut App) {
        
    }
}
